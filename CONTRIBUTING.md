# Contributing

This project is early.

Good first issues:

- Add more OpenAI-compatible fields
- Add scenario validation
- Improve streaming compatibility
- Add snapshot testing
- Add LangChain examples
- Add GitHub Action

Development:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pytest
```
